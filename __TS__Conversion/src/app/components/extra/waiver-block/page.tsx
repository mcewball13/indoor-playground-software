import {HTMLBlock} from 'src/sections/waiver/view/';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Waiver:HTML Block',
};

export default function UploadPage() {
  return <HTMLBlock/>;
}
